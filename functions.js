function first(a,b){ // parameters
    console.log("hi");
    // console.log(a-b);
    return a-b;
}

// res = first(1,2);  // arguments
// console.log(res);

function check(va1, val22, ...num){ 
    return num
}

// console.log(check(100,200))

const addTwo = (num1, num2) => {
     return num1 + num2
}

const adddTwo = (num1, num2) =>  num1 + num2
